const API='https://offjdeutxvcrybniftyl.supabase.co/functions/v1/admin-agenda',KEY='kb3_contact_admin_token',RAW='https://raw.githubusercontent.com/sdnkalibaru34-web/sdnkalibaru3depok.sch.id/main/',q=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));let token=localStorage.getItem(KEY)||'',items=[],currentId='';async function api(p){const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`},body:JSON.stringify(p)}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Permintaan gagal');return d}const slug=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,170),imageUrl=u=>{if(!u)return'';const s=String(u).trim();if(/^https?:\/\//i.test(s))return s.replace(/^https:\/\/cdn\.jsdelivr\.net\/gh\/sdnkalibaru34-web\/sdnkalibaru3depok\.sch\.id@main\//i,RAW);return RAW+s.replace(/^\.\.\//,'').replace(/^\.\//,'').replace(/^\//,'')};function auth(x){q('#login').hidden=x;q('#app').hidden=!x}async function load(){items=(await api({action:'list'})).items||[];render()}function filtered(){const s=q('#search').value.toLowerCase(),st=q('#filter-status').value,a=items.filter(x=>(!st||x.status===st)&&(!s||[x.title,x.summary,x.body,x.location].join(' ').toLowerCase().includes(s)));return window.KB3AdminSort?KB3AdminSort.apply(a,x=>x.start_date,x=>x.title):a}function render(){const a=filtered();q('#count').textContent=a.length===items.length?`${items.length} agenda`:`${a.length} dari ${items.length} agenda`;q('#list').innerHTML=a.length?a.map(x=>`<button class="item${x.id===currentId?' active':''}" data-id="${esc(x.id)}"><span class="badge">${x.status==='publish'?'Publish':'Draft'}</span><h3>${esc(x.title)}</h3><p>${esc(x.start_date)}</p></button>`).join(''):'<div style="padding:24px" class="muted">Tidak ada agenda.</div>';document.querySelectorAll('.item').forEach(b=>b.onclick=()=>edit(b.dataset.id))}function preview(u){const s=imageUrl(u);return s?`<img src="${esc(s)}" style="display:block;width:100%;max-width:380px;aspect-ratio:16/9;object-fit:contain;border-radius:14px;background:#f5f8f7;border:1px solid #d7e3df">`:''}function form(x={}){currentId=x.id||'';q('#detail').innerHTML=`<div class="section-label">${x.id?'Edit Agenda':'Agenda Baru'}</div><h2>${esc(x.title||'Tambah agenda & kegiatan')}</h2><form id="form" class="admin-form"><div class="form-grid"><label>Tanggal Mulai<input id="start" type="date" required value="${esc(x.start_date||new Date().toISOString().slice(0,10))}"></label><label>Tanggal Selesai<input id="end" type="date" value="${esc(x.end_date||'')}"></label></div><label>Waktu<input id="time" value="${esc(x.time_text||'')}" placeholder="07:00 - selesai"></label><label>Judul<input id="title" required maxlength="250" value="${esc(x.title||'')}"></label><label>ID / slug<input id="id" value="${esc(x.id||'')}" placeholder="otomatis-dari-judul"></label><label>Lokasi<input id="location" value="${esc(x.location||'')}"></label><label>Ringkasan<textarea id="summary" rows="3">${esc(x.summary||'')}</textarea></label><label>Isi Kegiatan<textarea id="body" rows="10">${esc(x.body||'')}</textarea></label><label>Upload Gambar<input id="image-file" type="file" accept="image/jpeg,image/png,image/webp"><small class="muted">JPG, PNG, WebP maksimal 5 MB. Otomatis masuk folder assets/images/agenda/.</small></label><div class="actions"><button id="upload" type="button" class="button secondary">Upload Gambar →</button><span id="upload-status" class="muted"></span></div><label>Path Gambar<input id="image" readonly value="${esc(x.image_url||'')}"></label><div id="preview">${preview(x.image_url)}</div><label>Status<select id="status"><option value="draft" ${x.status!=='publish'?'selected':''}>Draft</option><option value="publish" ${x.status==='publish'?'selected':''}>Publish</option></select></label><div class="actions"><button class="button">Simpan →</button>${x.id?'<button id="delete" type="button" class="button danger">Hapus</button>':''}<span id="status-text" class="muted"></span></div></form>`;q('#form').onsubmit=save;q('#upload').onclick=upload;if(x.id)q('#delete').onclick=remove}function edit(id){const x=items.find(i=>i.id===id);if(x)form(x)}function base64(f){return new Promise((res,rej)=>{const r=new FileReader;r.onload=()=>res(String(r.result).split(',')[1]);r.onerror=rej;r.readAsDataURL(f)})}async function upload(){const f=q('#image-file').files?.[0],s=q('#upload-status');if(!f)return s.textContent='Pilih gambar terlebih dahulu.';if(f.size>5*1024*1024)return s.textContent='Maksimal 5 MB.';s.textContent='Mengunggah...';try{const d=await api({action:'upload_image',file_name:f.name,mime_type:f.type,base64:await base64(f),title:q('#title').value,start_date:q('#start').value});q('#image').value=d.path;q('#preview').innerHTML=preview(d.path||d.raw_url);s.textContent='Upload berhasil.'}catch(e){s.textContent=e.message}}async function save(e){e.preventDefault();const s=q('#status-text');s.textContent='Menyimpan...';let id=q('#id').value.trim()||slug(q('#title').value);try{await api({action:'save',id,title:q('#title').value.trim(),start_date:q('#start').value,end_date:q('#end').value,time_text:q('#time').value,location:q('#location').value,summary:q('#summary').value,body:q('#body').value,image_url:q('#image').value,status:q('#status').value});currentId=id;await load();edit(id)}catch(e){s.textContent=e.message}}async function remove(){if(!confirm('Hapus agenda ini?'))return;await api({action:'delete',id:currentId});currentId='';await load();form({})}document.addEventListener('DOMContentLoaded',async()=>{window.KB3AdminSort?.bind(render);q('#login-form').onsubmit=async e=>{e.preventDefault();token=q('#token').value.trim();try{await api({action:'list'});localStorage.setItem(KEY,token);auth(true);await load();form({})}catch{q('#login-error').textContent='Token admin tidak valid.'}};q('#logout').onclick=()=>{localStorage.removeItem(KEY);token='';auth(false)};q('#new-item').onclick=()=>form({});q('#search').oninput=render;q('#filter-status').onchange=render;q('#reset-filter').onclick=()=>{q('#search').value='';q('#filter-status').value='';render()};if(token){try{await api({action:'list'});auth(true);await load();form({})}catch{auth(false)}}else auth(false)});