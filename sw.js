self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={title:'Pesan baru website SDN Kalibaru 3',body:event.data?.text()||''}}const title=data.title||'Pesan baru website SDN Kalibaru 3';const options={body:data.body||'Ada pesan baru di Ruang Komunikasi.',icon:'assets/images/logo-kb3.svg',badge:'assets/images/logo-kb3.svg',tag:data.tag||'kb3-contact',data:{url:data.url||'halaman/admin-pesan.html'}};event.waitUntil(self.registration.showNotification(title,options))});self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'halaman/admin-pesan.html',self.registration.scope).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list){if(client.url===target&&'focus'in client)return client.focus()}return clients.openWindow?clients.openWindow(target):undefined}))});